diff --git containers/BUILD containers/BUILD
--- containers/BUILD
+++ containers/BUILD
@@ -68,7 +68,7 @@
         "@com_github_grpc_grpc//:grpc++",
         "@com_google_absl//absl/status:status_matchers",
         "@com_google_absl//absl/time",
-        "@com_google_protobuf//:any_cc_proto",
+        "@com_google_protobuf//:protobuf",
         "@federated-compute//fcp/protos/confidentialcompute:confidential_transform_cc_proto",
         "@googletest//:gtest",
         "@googletest//:gtest_main",
@@ -138,8 +138,7 @@
         "@com_google_absl//absl/log:check",
         "@com_google_absl//absl/status",
         "@com_google_absl//absl/status:status_matchers",
-        "@com_google_protobuf//:differencer",
-        "@com_google_protobuf//:struct_cc_proto",
+        "@com_google_protobuf//:protobuf",
         "@federated-compute//fcp/base",
         "@federated-compute//fcp/base:compression",
         "@federated-compute//fcp/confidentialcompute:cose",
@@ -206,8 +205,7 @@
         "@com_google_absl//absl/status:statusor",
         "@com_google_absl//absl/strings",
         "@com_google_absl//absl/synchronization",
-        "@com_google_protobuf//:any_cc_proto",
-        "@com_google_protobuf//:protobuf_lite",
+        "@com_google_protobuf//:protobuf",
         "@federated-compute//fcp/base",
         "@federated-compute//fcp/base:status_converters",
         "@federated-compute//fcp/confidentialcompute:cose",
@@ -237,8 +235,7 @@
         "@com_google_absl//absl/log:check",
         "@com_google_absl//absl/status:status_matchers",
         "@com_google_absl//absl/strings:str_format",
-        "@com_google_protobuf//:protobuf_lite",
-        "@com_google_protobuf//:struct_cc_proto",
+        "@com_google_protobuf//:protobuf",
         "@federated-compute//fcp/base:status_converters",
         "@federated-compute//fcp/protos/confidentialcompute:confidential_transform_cc_grpc",
         "@federated-compute//fcp/protos/confidentialcompute:confidential_transform_cc_proto",
diff --git containers/kms_encryptor.cc containers/kms_encryptor.cc
--- containers/kms_encryptor.cc
+++ containers/kms_encryptor.cc
@@ -48,7 +48,7 @@
   FCP_ASSIGN_OR_RETURN(OkpKey okp_key, OkpKey::Decode(reencryption_key));
 
   BlobHeader header;
-  header.set_blob_id(blob_id);
+  header.set_blob_id(std::string(blob_id));
   header.set_key_id(okp_key.key_id);
   header.set_access_policy_sha256(reencryption_policy_hash_);
   return header.SerializeAsString();
@@ -63,14 +63,15 @@
 
   BlobMetadata::HpkePlusAeadMetadata* hpke_plus_aead_metadata =
       metadata.mutable_hpke_plus_aead_data();
-  hpke_plus_aead_metadata->set_ciphertext_associated_data(associated_data);
+  hpke_plus_aead_metadata->set_ciphertext_associated_data(
+      std::string(associated_data));
   hpke_plus_aead_metadata->set_encrypted_symmetric_key(
       encrypted_message.encrypted_symmetric_key);
   hpke_plus_aead_metadata->set_encapsulated_public_key(
       encrypted_message.encapped_key);
   hpke_plus_aead_metadata->mutable_kms_symmetric_key_associated_data()
-      ->set_record_header(associated_data);
-  hpke_plus_aead_metadata->set_blob_id(blob_id);
+      ->set_record_header(std::string(associated_data));
+  hpke_plus_aead_metadata->set_blob_id(std::string(blob_id));
   return metadata;
 }
 
diff --git containers/confidential_transform_server_base.cc containers/confidential_transform_server_base.cc
--- containers/confidential_transform_server_base.cc
+++ containers/confidential_transform_server_base.cc
@@ -196,7 +196,7 @@
 
   // TODO: Avoid flattening the cord, which requires the downstream
   // code to parse directly from the chunked cord.
-  absl::Cord data = request.data();
+  absl::Cord data(std::string(request.data()));
   // Clear the memory used by the original encrypted data.
   request.set_data("");
 
@@ -312,7 +312,7 @@
   }
 
   {
-    absl::MutexLock l(mutex_);
+    absl::MutexLock l(&mutex_);
     if (decryptor_ != std::nullopt) {
       return absl::FailedPreconditionError(
           "StreamInitialize can only be called once.");
@@ -348,7 +348,7 @@
 absl::Status ConfidentialTransformBase::SessionImpl(SessionStream* stream) {
   Decryptor* decryptor;
   {
-    absl::MutexLock l(mutex_);
+    absl::MutexLock l(&mutex_);
     if (decryptor_ == std::nullopt) {
       return absl::FailedPreconditionError(
           "Initialize must be called before Session.");
@@ -471,7 +471,7 @@
     grpc::ServerReaderWriter<SessionResponse, SessionRequest>* stream) {
   SessionTracker* session_tracker;
   {
-    absl::MutexLock l(mutex_);
+    absl::MutexLock l(&mutex_);
     if (session_tracker_ == std::nullopt) {
       return ToGrpcStatus(absl::FailedPreconditionError(
           "StreamInitialize must be called before Session."));
@@ -502,7 +502,7 @@
 }
 
 absl::StatusOr<Decryptor*> ConfidentialTransformBase::GetDecryptor() {
-  absl::MutexLock l(mutex_);
+  absl::MutexLock l(&mutex_);
   if (decryptor_ == std::nullopt) {
     return absl::FailedPreconditionError(
         "Initialize must be called before GetDecryptor.");
diff --git containers/session_stream.h containers/session_stream.h
--- containers/session_stream.h
+++ containers/session_stream.h
@@ -22,6 +22,7 @@
 #include <memory>
 
 #include "absl/status/status.h"
+#include "absl/status/statusor.h"
 #include "fcp/protos/confidentialcompute/confidential_transform.pb.h"
 #include "grpcpp/support/sync_stream.h"
 
diff --git containers/session_stream.cc containers/session_stream.cc
--- containers/session_stream.cc
+++ containers/session_stream.cc
@@ -67,9 +67,9 @@
         return FailedContinuationError();
       }
 
-      absl::Cord data = compatible_first_request->data();
-      data.Append(request->data());
-      compatible_first_request->set_data(data);
+      absl::Cord data(std::string(compatible_first_request->data()));
+      data.Append(absl::string_view(request->data()));
+      compatible_first_request->set_data(std::string(data.Flatten()));
     } else {
       // This is a new blob i.e. the first chunk of a multi-chunk blob or a
       // small blob consisting of a single chunk of data.
@@ -141,16 +141,16 @@
     ReadResponse* mutable_read = response.mutable_read();
     mutable_read->set_finish_read(mutable_read->data().size() <= chunk_size_);
     if (!mutable_read->finish_read()) {
-      absl::Cord data = mutable_read->data();
+      absl::Cord data(std::string(mutable_read->data()));
       do {
-        mutable_read->set_data(data.Subcord(0, chunk_size_));
+        mutable_read->set_data(std::string(data.Subcord(0, chunk_size_).Flatten()));
         data.RemovePrefix(chunk_size_);
         FCP_RETURN_IF_ERROR(WriteImpl(response));
         mutable_read->clear_first_response_configuration();
         mutable_read->clear_first_response_metadata();
       } while (data.size() > chunk_size_);
 
-      mutable_read->set_data(std::move(data));
+      mutable_read->set_data(std::string(data.Flatten()));
       mutable_read->set_finish_read(true);
     }
   }
