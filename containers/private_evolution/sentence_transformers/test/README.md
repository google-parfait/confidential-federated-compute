# Files that are used in tests

## my_model.zip
This zip file contains a directory that has the following structure:
-my_model
--model.config
--tokenizer.config

model.config file has the following content:
NAME: Bert

tokenizer.config file has the following content:
NAME: sentence-piece

NOTE: this archive is not a valid artifact directory for sentence-transformer, 
please don't use it for testing sentence-transformer.